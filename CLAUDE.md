# CLAUDE.md — 実装役向け常設ルール（kinsen-kanri）

このリポジトリで作業する Claude Code（実装役）は、依頼文より先にこのファイルを読むこと。
根拠と理由は `docs/00_開発ルール（相談役・実装役体制）.md`、全体仕様は `docs/10_仕様書_v2再設計.md`。

## 絶対に壊さないもの
- `main` ブランチ（本番。友人グループが利用中）— コミット・push・マージ禁止。作業はブランチ `v2`
- Firebase 旧パス `rooms/…`（友人の実データ）— 書き込み禁止。読み取りも移行工事（工事2）以外は禁止
- 利用者端末の localStorage キー `warikancho_v1` — 読まない・書かない・消さない
- Firebase セキュリティルール — 変更しない。変更案は報告書に書く
- 動作確認のグループ名は「テスト」で始める。実在の共有コード（Y39EN6 等）は使わない

## 編集のしかた（B-1 / B-3）
- 既存ファイルを Edit ツール等のテキスト置換で直接編集しない
- 既存ファイルの変更は Python スクリプト（`tools/` に保存、UTF-8 明示）で行う。置換対象の一致が「ちょうど 1 つ」であることを検証し、違えば何も書かずに終了する
- 編集後は `node --check <js>` で構文チェック。HTML は開閉タグの整合を確認
- 新しい機能は新規ファイル（`js/*.js`、`css/*.css`）に置き、`index.html` の変更は最小限（script/link タグの追加、DOM の必要最小限）
- 新規ファイルの作成は Python でも Write ツールでも可

## Git（B-2）
- 着手前に `git status` がきれいであることを確認。汚れていれば作業せず報告
- 工事の最初に `v2` ブランチへ移動（無ければ作成）。動いた状態ごとにコミット（1 行目 50 字以内、日本語可）
- 工事の終わりに `git push -u origin v2`

## テスト（C-1 / C-2）
- テストは `node tests/run_all.js` で一括実行し、全通過させる（`tests/*.test.js` を自動で拾う）
- 純粋ロジックは DOM・Firebase に依存しない形で `js/` に置く（按分・残高・清算は `js/calc.js`、旧データの変換は `js/migrate.js`）。新しいテストは `tests/<名前>.test.js` として足せば `run_all` が拾う
- ロジックに触る前に、現状の挙動を固定するテストを先に書く
- 直したバグは再現テストを追加してから直す
- 画面の通し確認（ヘッドレス Chrome）は毎回新しい一時フォルダを --user-data-dir に指定して起動する（同じプロファイルだと http.server から配った古い js がキャッシュされる）
- js/store.js の書き込み（パスとキー）は tests/store.test.js が偽の Firebase で固定している。store.js の書き込みを変える工事では、先にこのテストを現状で通し、変更後に期待値を直す

## この PC の環境（2026-09-23 工事1 で判明）
- Python は `py -X utf8 …` で呼ぶ（`python` は Microsoft Store のダミーで動かない）。Node.js は v24 導入済み
- ローカルサーバは `py -m http.server 8000`
- 配線チェック `py -X utf8 tools/check_wiring.py`、画面の通し確認 `py -X utf8 tools/make_uitest.py` → `http://localhost:8000/_uitest_tmp.html`（タイトルが `UI-SMOKE-PASS` なら通過）を、工事の終わりに毎回回す

## 日本語 Windows での注意
- 日本語を含む文字列を `echo` やコンソールのリダイレクトでファイルに書かない（cp932 で化ける）
- .bat / .cmd を作らない・使わない
- ファイルは UTF-8（BOM 無し）。コンソール表示が化けても、ファイルを読んで判定する
- 日本語文字列は数値実体参照（&#x…;）にせず UTF-8 でそのまま書く

## 完了の定義と報告（C-3 / C-5）
- 「実装完了」＝ブラウザの画面から利用者が使える状態。CLI や関数単体で動く段階は「機能完成（組込待ち）」と書く
- 迷ったら勝手に決めず、報告書の「要判断」に書く
- 工事の終わりに `docs/reports/工事N_報告.md` を UTF-8 で保存する。項目: 実装した／しなかった、仕様からずらした点と理由、自己確認の結果、ルール変更案、要判断、花井の検収手順、既知の問題と申し送り
- スクリーンショットは不要。報告書は相談役が直接読む
