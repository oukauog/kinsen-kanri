# 工事1 報告（実装役 Claude Code）— 2026-09-23

- ブランチ: `v2`（`main` には一切コミットしていない。`main` は着手時のまま `ab9a5e1`）
- コミット: `44810e2`（計算ロジックの切り出しとテスト）、`7dedc18`（v2 本体）、本報告のコミット
- 前提確認: `node -v` → v24.19.0（導入済み。winget は不要）。Python は `python` が Microsoft Store のダミーだったため **`py` ランチャ（Python 3.10.11）** を使用

---

## 1. 完了の言い方

| 範囲 | 状態 |
|---|---|
| ログイン画面 / ログアウト / 起動中画面 | **実装完了**（実 Firebase で未ログイン状態まで実機確認済み） |
| グループ一覧・作成・コードで参加・退出・削除 | **実装完了**（ただし後述のとおり、**実 DB での認証後の動作は未検証**） |
| 支払いの追加・編集・削除、履歴、残高、清算リスト表示 | **実装完了**（同上） |
| グループ設定（名前変更・メンバー追加/名前変更・共有コード表示とコピー） | **実装完了**（同上） |
| 接続状態インジケータ | **実装完了**（`.info/connected` が `true` になり色が変わるところまで実機確認済み） |
| 清算の記録（settlements）・金額確認中・未清算/累計切替 | 未実装（工事3 の範囲） |
| CSV 出力・xlsx 取込・旧 rooms 移行・誰が入力したか表示 | 未実装（工事1 の範囲外。工事2・工事4） |

**正直に書く注意点**: Google ログインは人の操作（アカウント選択）が要るため、実装役の環境では**ログイン後の Firebase 読み書き・2 端末同時反映を実データで確認できていません**。匿名ログインでの自動確認も試しましたが、プロジェクト側で無効（`auth/admin-restricted-operation`）のため不可でした。代わりに、Firebase を同じ形のメモリ実装に差し替えた**画面の通し確認（41 項目）をヘッドレス Chrome で全通過**させています（§5）。ルールと実 DB への書き込み順序が想定どおりかは、**花井の検収が最初の実地確認**になります。

---

## 2. ファイル一覧

### 新規（v2 本体）
| ファイル | 中身 |
|---|---|
| `js/calc.js` | 按分・残高・清算の純粋ロジック（v1 から切り出し）。ブラウザは `window.Calc`、Node は `module.exports` |
| `js/firebase-init.js` | `firebaseConfig` と `firebase.initializeApp`。`window.KKFirebase` に app/auth/db を公開 |
| `js/auth.js` | Google ログイン（`signInWithPopup` → 失敗時 `signInWithRedirect`）、ログアウト、`onAuthStateChanged` |
| `js/store.js` | `users/{uid}/…`・`groups/{code}/…` の読み書きと監視。すべてレコード単位の `set`/`update`/`remove` |
| `js/ui.js` | 画面全部。`index.html` の `onclick` から呼ぶ関数を `window` に生やす |
| `css/v2.css` | ログイン画面・ユーザー欄・接続ドット・トースト・共有コード等、v1 に無かった部品だけ |

### 新規（テストと道具）
| ファイル | 中身 |
|---|---|
| `tests/calc.test.js` | `docs/30_テストケース_清算.md` を期待値にした挙動固定テスト（Node 標準 `assert` のみ） |
| `tests/ui_smoke_fakes.js` | Firebase を差し替えるメモリ実装（本番の `index.html` からは読み込まれない） |
| `tests/ui_smoke_driver.js` | 画面を自動操作して 41 項目を確認するドライバ |
| `tools/edit_index_v2.py` | `index.html` の機械置換（本体） |
| `tools/edit_index_boot.py` | `index.html` に起動中画面を追加 |
| `tools/make_uitest.py` | `index.html` から確認用ページ `_uitest_tmp.html` を生成（生成物は git 管理外） |
| `tools/check_wiring.py` | HTML の `onclick` 名と `ui.js` の global、参照 id の突き合わせ |
| `.gitignore` | 生成物 `_uitest_tmp.html` / `_diag_tmp.html` を除外 |

### 変更したファイル
`index.html` のみ（1 ファイル）。すべて Python スクリプト経由で、**置換対象の一致がちょうど 1 件であることを検証してから**書き戻しています（1 件でも違えば何も書かずに終了する実装）。

| # | 変更 | スクリプト |
|---|---|---|
| ① | `</head>` 直前に `css/v2.css` の `<link>` を追加 | `tools/edit_index_v2.py` |
| ② | ヘッダ右の旧「☁ 設定」バッジ（`sync-bar`）を撤去し、接続ドット・アイコン・表示名・ログアウトに差し替え | 同上 |
| ③ | ログイン画面 `#loginScreen` を追加し、`<div class="layout">` に `id="appLayout" hidden` を付けて出し分け | 同上 |
| ④ | サイドバーの「＋ グループを作成」の下に「コードで参加」を追加 | 同上 |
| ⑤ | グループ設定モーダルに「共有コード（タップでコピー）」と「退出」を追加 | 同上 |
| ⑥ | v1 の「同期設定」モーダルを撤去し、「コードで参加」モーダルに差し替え | 同上 |
| ⑦ | v1 のインライン `<script>…</script>`（約 600 行）を全撤去し、Firebase compat 3 本 + `js/*.js` 5 本の読み込みに差し替え | 同上 |
| ⑧ | 起動中（ログイン状態が判明するまで）の画面 `#bootScreen` を追加 | `tools/edit_index_boot.py` |

- `index.html` は 973 行 → 414 行。`<div>` の開閉は 76 / 76 で一致、`<script>` は 8 / 8 で一致
- CSV 出力・取込のボタンは**描画しない**ことで非表示にしています（モーダルの DOM と v1 のスタイルは残置。工事4 で復元しやすいように）

---

## 3. テストと構文チェックの結果

### `node tests/calc.test.js`（全 16 件通過、終了コード 0）

```
ケース1: 4人・36件
  ok   件数が 36 件
  ok   残高（丸めなし）が仕様書の表と一致する
  ok   払った合計・負担分が仕様書の表と一致する
  ok   支払い総額が 197267 円
  ok   残高の合計が 0（誤差 1e-6 以下）
  ok   清算は 3 本、金額も順序も仕様書どおり
  ok   members / payments を ID キーのオブジェクトで渡しても同じ結果（v2 のデータ形）
ケース2: 端数と境界
  ok   2 人で 1 円 → 残高 +0.5 / -0.5、清算は 0 本（閾値 0.5 の挙動）
  ok   3 人で 100 円 → +66.67 / -33.33 / -33.33、清算は 2 本で各 33 円
  ok   支払者が割り勘対象に含まれない → 支払者に加算しない（v1 の癖。残高合計が 0 にならない）
  ok   割り勘対象 0 人の支払いは無視する
  ok   支払いが 0 件 → 全員 0、清算 0 本
  ok   メンバー一覧にいない人が対象に混ざっても計算できる（v1 と同じく動的に追加）
  ok   participants が {mid:false} のキーは対象外として扱う
表示用の小道具
  ok   share(): 1 人あたりの負担額（丸めない）
  ok   money(): 絶対値を四捨五入して 3 桁区切り（v1 と同じ）

すべて通過: 16 件
```

ケース1 の残高（A +35036.92 / B −35472.25 / C +26473.92 / D −26038.58）と送金（B→A 35037、B→C 435、D→C 26039、この順で 3 本）は仕様書の表そのままで通っています。

### 構文チェック
- `node --check` : `js/calc.js` `js/firebase-init.js` `js/auth.js` `js/store.js` `js/ui.js` `tests/calc.test.js` `tests/ui_smoke_fakes.js` `tests/ui_smoke_driver.js` → **全件 OK**
- HTML: 開閉タグ数一致（`<div>` 76/76、`<script>` 8/8）、`</body></html>` 残存を機械確認
- `tools/check_wiring.py` → **OK**（HTML から呼ぶ 18 関数すべてが `ui.js` の global にあり、`ui.js` が探す id がすべて HTML にある）。工事4 で復元予定の `downloadTemplate` / `handleImport` / `saveGroupRename` は「画面から開けない死に DOM」として既知扱い

---

## 4. 仕様書からずらした点と理由

| # | ずらした点 | 理由 |
|---|---|---|
| 1 | 共有コードの生成を「A–Z0–9 から 6 文字」に固定し、発行前に `meta` を読んで重複チェック（v1 は `Math.random().toString(36).slice(2,8)` で、まれに 6 桁未満になる） | 仕様は「6 桁英数大文字」。v1 の式は桁落ちの可能性があるため、桁数を保証する形にした |
| 2 | `calcSettlement` の戻り値に `fromId` / `toId` を追加（`from` / `to` は v1 と同じ「名前」のまま） | 工事3 で送金を `settlements/{sid}/transfers` に記録するとき ID が要る。v1 の値は 1 つも変えていない |
| 3 | `calc.js` は members / payments を「配列（v1 形）」「ID キーのオブジェクト（v2 形）」どちらでも受ける | 同じ関数で v1 の期待値テストと v2 の画面の両方を賄うため |
| 4 | 起動中画面 `#bootScreen` を追加（仕様に無い） | ログイン状態が分かるまでの 0.5〜1 秒が真っ白になっていた。実機（ヘッドレス）で確認して追加 |
| 5 | 支払い履歴の行に「割り勘対象の名前」を表示（v1 は CSS だけあって未表示） | 誰で割ったかが行だけで分かるようにした。スマホでは 1 行省略表示。不要なら外します（→ 要判断） |
| 6 | グループ設定のメンバー欄を「入力欄で直接リネーム」に変更（v1 は表示＋削除のみ） | 仕様 §5.2 の「メンバー追加・名前変更」を満たすため |
| 7 | グループ削除時、**他メンバーの `users/{uid}/groups/{code}` は消せない**（ルール上、他人の `users` 配下に書けない）。相手の画面ではサイドバーに「（削除されたグループ）」と出て、× で自分の一覧から消せる | ルールを変えずに成立させるための設計。仕様には書かれていない挙動なので明記します |
| 8 | 監視は `groups/{code}` の `value` 丸ごと 1 本（child 系に分けていない） | 1 グループ数百件なら十分軽い。重くなったら child 系へ（申し送り） |
| 9 | 残高は「全支払い」で計算（未清算分に限定していない） | `settlementId` の切替は工事3 の範囲。データには今から `settlementId: null` / `pending: false` を書いている |

---

## 5. 自己確認の結果（やったこと・分かったこと）

### 5-1. 実 Firebase での確認（ヘッドレス Chrome + `py -m http.server 8000`）
本物の `index.html` に確認用の計測だけを足したページを作って実行（結果はローカルサーバのアクセスログ経由で回収）。

| 確認したこと | 結果 |
|---|---|
| gstatic から Firebase compat 3 本が読める | **OK**（`typeof firebase === 'object'`） |
| `firebase.initializeApp` が通り `KKFirebase.ready === true` | **OK** |
| `Calc` / `KKStore` / `KKAuth` が読み込まれている | **OK** |
| JS エラー・未処理の Promise 拒否 | **0 件** |
| `.info/connected` が `false` → `true` になり、接続ドットの灰色が消える | **OK**（RTDB への接続が実際に張れている） |
| `onAuthStateChanged` が `null` で発火し、ログイン画面に切り替わる | **OK**（起動中画面 → ログイン画面、`btnLogin` 表示） |
| 未ログインで `groups/…/meta` を読む | **PERMISSION_DENIED**（ルールが効いている。想定どおり） |
| 匿名ログインで認証後の読み書きを自動確認 | **不可**（`auth/admin-restricted-operation`。匿名プロバイダが無効） |

### 5-2. 画面の通し確認（Firebase をメモリ実装に差し替え、ヘッドレスで自動操作）
`py -X utf8 tools/make_uitest.py` → `http://localhost:8000/_uitest_tmp.html` を開くと自動で走ります（タイトルが `UI-SMOKE-PASS` になれば通過）。

**41 項目すべて通過**。内訳（抜粋）:
- ログイン状態が分かるまで起動中画面 / ログイン後にアプリ画面へ切替 / ヘッダに表示名
- グループ作成 → サイドバーに出る → 画面が開く → 残高カード 3 枚
- 支払い記録 → 履歴 1 件、メモと金額、残高が +¥2,000 / −¥1,000 / −¥1,000
- 編集（3000→6000）が反映され件数が増えない / 削除で 1 件減る
- **他端末からの追加が画面に入り、その行だけハイライトされる**
- 清算モーダルに送金リストが出る
- 設定: 共有コード表示、グループ名変更、メンバー名変更、メンバー追加（カード 4 枚）
- コードで参加（小文字入力でも大文字に正規化）、存在しないコードはエラー文言
- 退出で一覧から消える／グループのデータは残る、ログアウトでログイン画面に戻る

この自動確認で実際に 2 件のバグを見つけて直しました。
1. **新着行のハイライトが消える**: 「前回描画に無かった行」で判定していたため、直後に別のリスナーが再描画すると消えていた。「最初に見えた時刻から 1.6 秒」に変更
2. **起動直後が真っ白**: ログイン状態が判明するまでログイン画面もアプリ画面も出ない時間があった。起動中画面を追加

### 5-3. やっていない確認（正直に）
- **Google ログイン本番（アカウント選択のポップアップ）** — 人の操作が必要。未確認
- **認証後の実 DB 読み書き**（グループ作成・支払い保存・2 端末同時反映・参加・退出・削除がルール下で通るか） — 匿名ログインが無効のため自動確認できず。**メモリ実装での通し確認までしかしていません**
- ポップアップ拒否時の `signInWithRedirect` フォールバック — 実機未確認
- スマホ実機での見た目（v1 のスマホ用 CSS はそのまま残し、追加分だけ 640px 以下の指定を書いています）

---

## 6. Firebase ルールへの変更提案

**工事1 の実装は、仕様書 §4 の「開発中ルール」のままで動く前提で書いています。変更は不要と考えています。**（ただし §5-3 のとおり実地未検証）

前提として満たしてほしいこと（すでに適用済みなら確認だけ）:
1. `groups/$code/meta` が `".read": "auth != null"` であること — 「コードで参加」の確認とサイドバーの名前表示に必須
2. `users/$uid` が本人のみ読み書きであること — 実装は「先に `users/{uid}/groups/{code}` を書いてから `groups/{code}` を触る」順序にしています（逆だと権限が無く書けません）。グループ削除も「`groups/{code}` を消してから自分の一覧を外す」順序です

任意の提案（今すぐでなくてよい）:
- `users/$uid/groups/$code` の書き込みに `".validate": "newData.hasChild('joinedAt')"` を足すと、想定外の形のデータが入らなくなります
- 型の検証（`amount` が数値、`participants` がオブジェクト等）は、金額確認中（`pending`）が入る工事3 と合わせて入れるのが素直だと思います
- **本番切替まで `rooms` は v1 のまま（`".read": true, ".write": true`）**で問題ありません。v2 のコードは `rooms` を一切参照していません（`grep` で確認済み）

---

## 7. 要判断（相談役・花井に決めてほしいこと）

1. **Vercel プレビュードメインの承認済みドメイン追加**（検収の前提）: プレビュー URL（`kinsen-kanri-git-v2-….vercel.app` 形式）を Firebase Authentication の「承認済みドメイン」に追加しないと、プレビューでは Google ログインが `auth/unauthorized-domain` で必ず失敗します。花井の作業です
2. **グループ削除の権限**: 現状は仕様 §10 の暫定どおり「メンバーなら誰でも削除可」。作成者のみに絞るならルール変更が要ります（`meta/createdBy` との照合）
3. **削除されたグループの後始末**: 他人が削除したグループは、相手の一覧に「（削除されたグループ）」として残り、× で自分で消す形にしました。自動で消す（＝他人の `users` 配下を触る）にはルール変更が必要です。この UI でよいか
4. **メンバー削除の扱い**: 支払い履歴に登場するメンバーを削除すると、履歴の名前が「?」になります（v1 と同じ）。確認ダイアログは出しています。将来は「無効化フラグ」で残す案もあります
5. **支払い履歴の行に割り勘対象の名前を出すか**（§4-5）。スマホでは 1 行に収まらず省略表示になります
6. **共有コードを 6 桁固定にした点**（§4-1）の可否。既存の `Y39EN6` と同じ形なので移行に影響はありません
7. **Firebase SDK のバージョン固定**: `10.12.2` を URL 直書きで固定しています。上げる運用にするか、固定のままにするか
8. **`.gitignore` を新設**しました（確認用の生成物 2 つだけ）。不要なら外します

---

## 8. 花井の検収手順（PC とスマホの 2 台）

**手順 0（必須・先にやること）**
1. `git push` 済みの `v2` ブランチについて、Vercel の Deployments からプレビュー URL を控える
2. Firebase Console → Authentication → Settings → 承認済みドメイン に、そのプレビューのドメインを追加する（これを飛ばすとログインできません）

**手順（PC = A、スマホ = B）**
1. A でプレビュー URL を開く → 「読み込み中…」の後にログイン画面が出る → 「Google でログイン」→ 自分のアカウントでログイン
2. A: 左下「＋ グループを作成」→ 名前を **「テスト検収」** → メンバーを 3 人追加（例: 花井・A さん・B さん）→ 作成。※実在の共有コード（`Y39EN6` 等）は絶対に入力しないでください
3. A: 「設定」→ 共有コードをタップしてコピー（「コピーしました！」と出る）。コードを控える
4. B（スマホ、Safari か Chrome）で同じ URL → 同じ Google アカウントでログイン → **左のメニュー（☰）にサイドバーが出て「テスト検収」が並ぶこと**を確認
5. A: 「＋ 支払いを記録」→ 日付・メモ「宿代」・支払った人・金額 6000 → 記録 → **B の履歴と残高が数秒以内に変わること**（B は何も操作しない）
6. B: 同じ支払いの ✏ で金額を 8000 に編集 → **A 側が変わること**。続けて B で ✕ 削除 → **A から消えること**
7. A: 「清算」→ 送金リストが出ること（「この内容で清算する」ボタンはまだありません。工事3）
8. **別の Google アカウントがあれば** B でそのアカウントにログイン → 「コードで参加」→ 手順 3 のコードを入力 → 「『テスト検収』に参加しますか？」→ 参加 → 同じ画面が見えること。**参加していない他のグループが一切見えないこと**
9. B を機内モードにする → ヘッダの接続ドットが灰色になること → 機内モードを解除 → 緑に戻り、その間に A で足した支払いが入ること
10. 「設定」→「退出」→ 一覧から消えること。もう一度「コードで参加」→ データがそのまま残っていること
11. 「設定」→「グループを削除」→ 確認ダイアログ → 消えること。もう一方の端末では「（削除されたグループ）」と出るので × で消せること
12. 本番（`https://kinsen-kanri.vercel.app/`）を開き、**v1 のままで何も変わっていないこと**を確認（`main` は未変更）

うまくいかないときに見てほしいもの: ブラウザのコンソール（エラーは握りつぶさず、画面下に赤いトーストでも出ます）。「アクセスが拒否されました」と出た場合はルールか承認済みドメインの問題です。

---

## 9. 既知の問題・次の工事への申し送り

1. **認証後の実 DB 動作が未検証**（§5-3）。工事2 の最初に、花井の検収結果を見てから着手するのが安全です
2. `index.html` に v1 の**死に DOM** が残っています: 取込モーダル・CSV 出力モーダル・グループ名変更モーダル（どれも画面から開く導線はありません）。v1 の `.sync-*` CSS も残置。工事4 の「細部の UI 調整」で整理するのがよいと思います
3. **グループ作成が途中で失敗した場合**（`users` 側だけ書けて `groups` が書けなかった等）、サイドバーに「（削除されたグループ）」が出ます。× で消せますが、原子的ではありません。気になるなら工事2 で多パス `update` に変える手があります
4. **支払いの監視は `groups/{code}` の `value` 1 本**です。1 グループの支払いが数千件規模になったら `child_added` / `child_changed` / `child_removed` に分けてください
5. 清算は**表示のみ**です（記録・ロック・取り消しは工事3）。データには `settlementId: null` と `pending: false` を今から書いているので、工事3 は読み替え不要で足せます
6. 履歴の並びは `date` の降順 → `createdAt` の降順（v1 と同じ）。`createdAt` はサーバ時刻です
7. `js/calc.js` の挙動は v1 完全互換です（支払者が対象外のときの癖も含む）。**変える場合は必ず `tests/calc.test.js` を先に直してから**にしてください
8. 工事2 の移行では、旧 `rooms` の `participants` 配列 → `{mid:true}` 変換に `Calc.normalizeParticipants` がそのまま使えます
9. 確認用の道具は残してあります。`py -X utf8 tools/check_wiring.py`（配線チェック）と `py -X utf8 tools/make_uitest.py` → `_uitest_tmp.html`（画面の通し確認）は、工事2 以降も回してから報告するのがおすすめです

---

## 10. 守ったことの確認

- `main` へのコミット・push・マージ: **していない**（作業はすべて `v2`）
- Firebase 旧パス `rooms/…`: **読み書きしていない**（`grep` で参照 0 件）
- localStorage `warikancho_v1`: **触っていない**（v2 が使うのは `kk_v2_lastGroup` のみ）
- Firebase セキュリティルール: **変更していない**（提案のみ §6）
- 動作確認のグループ名: すべて「テスト」始まり。実在の共有コードは未使用（実 DB には認証できなかったため、そもそもデータを 1 件も作っていません）
- 既存ファイルの編集: `index.html` のみ、Python スクリプト経由・一致 1 件検証つき。Edit ツールでの直接編集は**この工事で自分が新規作成したファイルに限って**使用しました
- 日本語: ファイルはすべて UTF-8（BOM 無し）、`echo` やリダイレクトでの日本語書き込みなし、.bat/.cmd なし。新規に書いた日本語は数値実体参照にしていません（`index.html` の v1 由来部分は元の実体参照のまま）
