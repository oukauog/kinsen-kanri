# 工事4c 報告（実装役 Claude Code）— 2026-09-24

- ブランチ: `v2`（`main` には触れていない。`main` は `45755e3` のまま＝本番稼働中）
- 依頼された 1 件（スマホ幅で入力欄を 16px にする）だけを実装しました。範囲は広げていません
- `main` へのマージはしていません（依頼どおり）

## コミット

| コミット | 内容 |
|---|---|
| `9244c28` | スマホ幅で入力欄を16pxにしiOSの自動ズームを防ぐ |
| （本報告） | 工事4cの報告書を追加 |

### 着手時の `git status` について
`Claude outputs/kinsen-kanri_QR.png`（QR コードの画像）が未追跡でありました。
**ドキュメント（.md）ではなく、私の作業でもない**ため、勝手にコミットせず**未追跡のまま残しています**。
今回のコミットは `git add -A` を使わず、自分が触った 4 ファイルだけを明示的に指定しました。
リポジトリに入れるかどうかは花井の判断です（→ §6-1）。

---

## 1. 完了の言い方

| 項目 | 状態 |
|---|---|
| スマホ幅（640px 以下）で入力欄の文字を 16px にする | **実装完了**（幅 390px / 1024px の実測で確認済み） |
| iPhone 実機で実際にズームしなくなること | **未検証**（実機が無いため。→ §6-2 で花井の確認をお願いします） |

---

## 2. 変更ファイル

| ファイル | 変更内容 | スクリプト |
|---|---|---|
| `css/v2.css` | 既存の `@media (max-width: 640px)` ブロックの**中**に 16px の指定を追加（13 行） | `tools/edit_css_nozoom_4c.py` |
| `tests/ui_smoke_driver.js` | 通し確認に幅 390px / 1024px の文字サイズ確認（8 項目）を追加 | `tools/edit_uismoke_nozoom_4c.py` |

**触っていないもの**: `index.html`（viewport の `<meta>` を含む）、`main`、`rooms/…`、`warikancho_v1`、Firebase ルール、`firebaseConfig`、`vercel.json`、`js/` のすべて、`padding` や高さの指定。

---

## 3. 追加した CSS 規則の全文

`css/v2.css` の末尾、既存の `@media (max-width: 640px) { … }` の**内側**に足しました。

```css
  /* 工事4c: iOS Safari の自動ズーム対策。
     入力欄の文字が 16px 未満だとフォーカス時に画面が拡大され、
     body が position: fixed のため戻せなくなる。スマホ幅では 16px にする。
     index.html の .form-input（0.9rem）に勝たせるため、
     要素名 + クラス（詳細度 0,1,1）で指定する（!important は使わない）。
     padding は変えないので、欄の高さがわずかに増えるだけ。 */
  input.form-input,
  select.form-input,
  textarea.form-input,
  input.member-row-input {
    font-size: 16px;
  }
```

### 詳細度で勝つ根拠

| セレクタ | 場所 | 詳細度 | font-size |
|---|---|---|---|
| `.form-input` | `index.html` の `<style>`（102 行目） | **0, 1, 0** | 0.9rem = 14.4px |
| `.member-row-input` | `css/v2.css`（158 行目） | **0, 1, 0** | 0.88rem = 14.08px |
| `input.form-input` / `select.form-input` / `textarea.form-input` / `input.member-row-input` | `css/v2.css` の `@media` 内（今回追加） | **0, 1, 1** | 16px |

- 「要素名（タイプセレクタ）1 つ + クラス 1 つ」で **0,1,1**。既存はどちらもクラス 1 つだけの **0,1,0** なので、**詳細度で上回ります**（読み込み順に依存しません）
- `@media` は詳細度を上げも下げもしないので、勝敗は上の比較だけで決まります
- `!important` は使っていません（スクリプト側でも、追加する宣言に `!important` が含まれていないことを機械的に確認しています）
- `.form-input` は `<input>` だけでなく `<select>`（支払った人）にも付くため、`input` / `select` / `textarea` の 3 つを並べました。実際に `#payPayer`（select）が 16px になることも確認済みです（§4）

### 影響範囲
- `@media (max-width: 640px)` の中だけなので、**PC 幅（641px 以上）は一切変わりません**（実測で確認。§4）
- `padding: 9px 12px` はそのままなので、欄の高さが文字 1.6px 分だけ増えます（依頼どおり許容）
- viewport の `<meta>` は変更していません。`maximum-scale` / `user-scalable=no` は**追加していない**ので、利用者のピンチ拡大は従来どおり使えます

---

## 4. テスト・構文・配線・通し確認の結果

CSS のみの変更なので、既存のテスト結果が変わらないことの確認です。

| 種類 | 結果 |
|---|---|
| `node tests/run_all.js` | **6 ファイル全通過**（authdomain 8 / calc 16 / csv 25 / env 16 / migrate 26 / settle 25）＝工事4b から変化なし |
| `node --check js/*.js tests/*.js` | **全件 OK** |
| `py -X utf8 tools/check_wiring.py` | **OK**（HTML から呼ぶ 23 関数すべてが `ui.js` の global にある） |
| 画面の通し確認（ヘッドレス Chrome） | **150 項目すべて通過 / 失敗 0**（工事4b の 142 + 今回の 8） |

今回足した 8 項目（ヘッドレスではウィンドウ幅を変えられないため、同じページを **iframe に読み込んで幅を変え**、`getComputedStyle().fontSize` を実測しています）:

```
ok   スマホ幅: 日付の入力欄が 16px
ok   スマホ幅: メモの入力欄が 16px
ok   スマホ幅: 金額の入力欄が 16px
ok   スマホ幅: 支払った人の select が 16px
ok   スマホ幅: グループ名の入力欄も 16px
ok   スマホ幅: メンバー名の入力欄が 16px
ok   PC 幅: 入力欄は 14.4px のまま（見た目を変えない）
ok   PC 幅: メンバー名の入力欄も変えていない（14.08px）
```

### 取りこぼしが無いことの確認
ページ内の `input` / `select` / `textarea` を**全部走査**して、16px 未満の「文字入力欄」が残っていないかを調べました（チェックボックス・ラジオ・ファイル選択はタップしてもズームしないので対象外）。

| 幅 | 16px 未満の文字入力欄 |
|---|---|
| 390px（iPhone 相当） | **なし** |
| 1024px（PC 相当） | `newGroupName` / `newMemberInput` / `payDate` / `payMemo` / `payPayer` / `payAmount` / `editGroupName` / `editMemberInput` / `renameGroupInput` / `joinCodeInput` = 14.4px、`member-row-input` = 14.08px（**PC は従来どおりで正しい**） |

スマホ幅で 16px 未満の文字入力欄は **1 つも残っていません**。

### スクリプトが 2 回自分で止まった件（正直に）
`tools/edit_css_nozoom_4c.py` の安全確認が、最初 2 回とも作業を止めました。どちらもファイルは無傷です。
1. 「ファイル全体に `!important` が無いこと」を見ていたため、**工事1 からある `[hidden] { display: none !important; }`** に当たって停止
2. 直したあと、今度は**追加する文字列**を見たため、私が書いた**説明コメントの中の「`!important` は使わない」という文言**に当たって停止

最終的に「追加する宣言（コメントを除いた本体）に `!important` が無いこと」を見るように直して適用しました。確認が厳しすぎただけで、CSS の中身は最初から同じです。

---

## 5. 仕様からずらした点

ずらした点はありません。依頼文のとおりです。1 点だけ補足します。

- 依頼文は「`.form-input`（`input` / `select` / `textarea` のいずれであっても）」だったので、**3 つのタイプセレクタを列挙**しました。`.form-input` に要素名を付けずに `font-size` を上げる書き方（例: `.modal .form-input`）でも詳細度は足りますが、依頼文の意図（どの要素でも効かせる）がそのまま読める形を選びました

---

## 6. 花井の検収手順

### 6-1. 先に 1 つだけご判断ください
`Claude outputs/kinsen-kanri_QR.png` が未追跡のまま残っています。リポジトリに入れるなら次の工事で私がコミットします。入れないなら `.gitignore` に足すか、フォルダごと移動してください（放置でも動作に影響はありません）。

### 6-2. iPhone の Safari（本題）
プレビュー: `https://kinsen-kanri-git-v2-o-ka-s-projects.vercel.app/`
**「テスト」始まりのグループ**で試してください。

1. iPhone の Safari でプレビューを開いてログイン →「テスト」始まりのグループを開く
2. 「＋ 支払いを記録」→ **日付・メモ・金額を順にタップ**する
   - **画面が拡大しないこと**（これまでは拡大して、横にずれたまま戻せなくなっていました）
   - 「支払った人」のプルダウンも同様
   - 入力欄の文字が以前よりわずかに大きく、欄がわずかに高くなっています（意図どおりです）
3. モーダルを閉じて、画面の端が切れていない（横スクロールが起きていない）こと
4. 「設定」→ **メンバー名の入力欄をタップ** → 同じく拡大しないこと
5. 「コードで参加」のコード入力欄、「＋ グループを作成」のグループ名・メンバー名の欄でも拡大しないこと
6. **ピンチ操作での拡大は今までどおりできる**こと（自分で拡大する手段は残してあります）

### 6-3. PC で見た目が変わっていないこと
- PC のブラウザでプレビューを開き、各モーダルの入力欄の**文字サイズ・高さが今までと同じ**であること（PC 幅には一切手を入れていません）

### 6-4. 本番がまだ直っていないこと（比較用）
- `https://kinsen-kanri.vercel.app/` を iPhone の Safari で開き、同じ操作をすると**まだ拡大する**こと（`main` は未変更なので、これが正しい状態です）
- この工事をマージするかどうかは、検収後に別途ご依頼ください

---

## 7. 要判断・申し送り

1. **16px 未満の入力欄は、スマホ幅では残っていません**（§4 の全走査で確認）。今後 `index.html` や `ui.js` に新しい入力欄を足すときは、`class="form-input"` を付けておけば自動的に 16px になります。**付け忘れると同じ不具合が再発します**
2. **チェックボックス・ラジオ・ファイル選択**（`payPending`、送金チェック、取り込むグループの選択、`importFile`）は文字入力しないため対象外にしました。iOS はこれらのタップではズームしません
3. **欄の高さがわずかに増えます**（文字 14.4px → 16px、padding は据え置き）。支払いモーダルは縦に少し長くなります。詰めたい場合は、スマホ幅だけ `padding` を `8px 12px` などに減らす手がありますが、今回は依頼どおり触っていません
4. **根本の対処ではありません**。今回は「ズームさせない」ことで回避しています。`body { position: fixed; overflow: hidden }`（v1 からの作り）をやめれば、万一ズームしても戻せるようになりますが、レイアウト全体に影響する変更なので別工事として検討してください
5. **次の候補**（今回は触っていません）: ホーム画面アイコン（`manifest.json` / `apple-touch-icon`）、xlsx/CSV 取込の復元、2 端末同時の清算確定（工事3 §7-2）、スマホの見た目細部

---

## 8. 4c-2 修正（日付欄の揃え） — 2026-09-24

工事4c でズームは解消したものの、花井の iPhone 実機で **「支払いを記録」の日付欄（`#payDate`）だけ枠が不自然に伸びて**他の欄と揃わない、という報告を受けての修正です。

- コミット: `b2eb50c`（スマホ幅で入力欄の高さを揃え日付欄の伸びを直す）
- 変更ファイル: `css/v2.css`（`tools/edit_css_datefix_4c2.py`）、`tests/ui_smoke_driver.js`（`tools/edit_uismoke_datefix_4c2.py`、`tools/fix_uismoke_pcexpect_4c2.py`）
- `index.html`・`js/` は触っていません。`main` へのマージもしていません

### 8-1. 追加した CSS 規則の全文

`css/v2.css` の `@media (max-width: 640px)` の中、工事4c で足した規則の**直後**に追加しました。

```css
  /* 工事4c-2: 入力欄の高さを明示して揃える。
     iOS Safari は input[type="date"] を独自の内部部品で描くため、
     文字サイズ + padding から高さ・幅を素直に計算せず、日付欄だけ伸びていた。
     16px の文字 + 上下 padding 9px + 枠 1px を見込んで 40px に統一する
     （box-sizing: border-box が * で効いているので、これが外寸になる）。
     padding・border・角丸・色は変えない。 */
  input.form-input,
  select.form-input,
  input.member-row-input {
    height: 40px;
  }

  /* textarea は複数行なので高さを固定しない（現在このアプリには無い。将来用の保険）*/
  textarea.form-input {
    min-height: 40px;
  }

  /* 日付欄だけ iOS の独自描画を抑える。
     appearance を切って、幅と行の高さを他の欄と同じ土俵に乗せる。 */
  input[type="date"].form-input {
    -webkit-appearance: none;
    appearance: none;
    display: block;
    width: 100%;
    min-width: 0;
    line-height: normal;
  }

  /* iOS で日付の値が中央寄せになる既知の癖への対処（左寄せに揃える）*/
  input[type="date"].form-input::-webkit-date-and-time-value {
    text-align: left;
  }
```

### 8-2. 各指定の狙い

| 指定 | 狙い |
|---|---|
| `height: 40px`（input / select / member-row-input） | 外寸を数値で固定してしまい、ブラウザごとの高さ計算の差を無効にする。`box-sizing: border-box` が `*` で効いているので、これがそのまま枠の外寸になる |
| `textarea` だけ `min-height` | 複数行なので高さを固定すると入力が隠れる。**現在このアプリに `textarea` は 1 つもありません**（`index.html`・`js/ui.js` を検索して確認済み）。将来足したときに巻き込まれないための保険です |
| `-webkit-appearance: none; appearance: none;` | iOS が `type="date"` に当てる独自の内部部品の見た目・寸法計算を切り、ふつうのテキスト欄と同じ土俵に乗せる |
| `display: block; width: 100%; min-width: 0;` | iOS の日付欄は既定で `inline-block` 相当かつ内容に応じた最小幅を持つため、他の欄より広がったり縮んだりする。明示して幅を揃える |
| `line-height: normal` | 文字の縦位置がずれて枠が伸びるのを防ぐ |
| `::-webkit-date-and-time-value { text-align: left }` | iOS で日付の値が中央寄せになる既知の癖への対処。他の欄（左寄せ）と見た目を合わせる |

`padding`（9px 12px）・`border`・`border-radius`・色・`viewport` は変更していません。`!important` も使っていません（スクリプト側で、追加する宣言に `!important` と `padding` / `border` / `color` / `background` が含まれていないことを機械的に確認しています）。詳細度は `input[type="date"].form-input` が **0,2,1** で、`index.html` の `.form-input`（0,1,0）と工事4c の `input.form-input`（0,1,1）の両方を上回ります。

### 8-3. 通し確認の結果

**156 項目すべて通過 / 失敗 0**（工事4c の 150 + 今回の 6）。

幅を変えるためにページを iframe に読み込み、支払いモーダルを開いた状態で `getBoundingClientRect()` を実測しています。

**修正前後の実測値**（ヘッドレス Chrome）:

| 幅 | 4c-2 適用前 | 4c-2 適用後 |
|---|---|---|
| 390px（スマホ相当） | 日付 **46** / メモ 41 / 金額 41 / 支払った人 43（**バラバラ**） | **4 欄すべて 40 × 302**（揃った） |
| 1024px（PC 相当） | 日付 43 / メモ 40 / 金額 40 / 支払った人 42 | **同じ（43 / 40 / 40 / 42）＝無変更** |

今回足した確認:
```
ok   スマホ幅: 4 つの入力欄が測れる
ok   スマホ幅: 日付欄の高さが他の欄と揃う（4 欄が同じ高さ）
ok   スマホ幅: 4 欄の幅が揃う
ok   スマホ幅: 高さが 40px になっている
ok   PC 幅: 高さを 40px に固定していない（PC は従来のまま）
ok   PC 幅: 日付欄は Chrome 既定のまま（スマホ用の高さ指定が漏れていない）
```

その他: `node tests/run_all.js` **6 ファイル全通過**（CSS のみの変更なので工事4c から変化なし）、`node --check js/*.js tests/*.js` **全件 OK**、`tools/check_wiring.py` **OK**。

#### 途中で見つけたテスト側の誤り（正直に）
最初、依頼文にあった「幅 1024px では日付欄・メモ欄の高さが等しい」をそのまま期待値にしたところ **失敗しました**（日付 43px / メモ 40px）。

調べると、**Chrome は PC 幅でも日付欄を数 px 高く描いており、これは工事4c-2 の前からそう**でした（上の表の「適用前 1024px」）。今回の規則は `@media (max-width: 640px)` の中だけなので PC には届きません。つまり**実装ではなく期待値の思い込みが誤り**でした。

確認したいことは「PC が前と同じままか」なので、「高さが 40px に固定されていないこと」＋「日付欄が Chrome 既定どおりメモ欄より高いままであること」を見る形に直しました（`tools/fix_uismoke_pcexpect_4c2.py`）。判定の切り分けのため、**HEAD の CSS を使ったページを別に作って適用前の寸法も実測**しています。

#### ヘッドレス Chrome で確認できる範囲（重要）
今回の不具合は **iOS Safari 固有の描画**によるもので、ヘッドレス Chrome では再現しません。
上の確認で言えるのは「**Chrome では 4 欄の寸法が揃っており、PC 幅を崩していない**」までです。
**iOS 実機で実際に揃って見えるかどうかの判定は、花井の検収（§8-4）が唯一の確認手段**です。

### 8-4. 花井の検収手順

プレビュー: `https://kinsen-kanri-git-v2-o-ka-s-projects.vercel.app/`
**「テスト」始まりのグループ**で試してください。

**iPhone の Safari**
1. プレビューを開いてログイン →「テスト」始まりのグループを開く
2. 「＋ 支払いを記録」を開き、**日付・メモ・金額・支払った人の 4 欄が同じ高さ・同じ幅で揃っている**こと（日付欄だけ伸びていないこと）
3. **日付欄をタップしてもズームしない**こと（工事4c の効果が残っていること）
4. **日付ピッカーが今までどおり開き、選んだ日付が欄に表示される**こと（`appearance: none` で操作が壊れていないこと。ここが今回いちばん確認していただきたい点です）
5. 日付の文字が**左寄せ**で表示されること（中央寄せになっていないこと）
6. 「＋ グループを作成」「設定」「コードで参加」の各入力欄も、高さが揃っていること
7. メンバー名の入力欄（設定の中）も高さが揃っていること

**PC**
8. PC のブラウザで各モーダルを開き、**見た目が今までと変わっていない**こと（PC 幅には手を入れていません）

**本番との比較**
9. `https://kinsen-kanri.vercel.app/` は `main` のままなので、**まだ日付欄が伸びている**のが正しい状態です

### 8-5. 要判断・申し送り

1. **`-webkit-appearance: none` による日付欄の見え方の変化**（実機で確認してください）。iOS ではこの指定でカレンダーのアイコンや内部の装飾が消える／薄くなることがあります。**ピッカー自体はタップで開きます**が、「日付欄だとひと目で分かりにくい」と感じる場合は、次のどちらかで戻せます:
   - `appearance: none` をやめて `height` だけで揃える（揃いは弱くなるが装飾は残る）
   - 欄の左にカレンダーの絵文字を置く等、別の方法で日付欄だと示す
   どちらが良いかは実機の見た目を見てご判断ください
2. **PC 幅では日付欄が他より 3px 高いまま**です（Chrome 既定。工事4c より前からそうで、今回は対象外）。PC も揃えるなら、`@media` の外に同種の指定が要ります。気になるようなら次の工事で
3. **高さ 40px は固定値**です（`css/v2.css` のスマホ用ブロック）。padding を変えずに高さだけ決めているので、将来 `padding` を変えるときは 40px も見直してください
4. **`Claude outputs/` は未追跡のまま触っていません**（依頼どおり。`.gitignore` での扱いは別途）
5. 今回も**根本対処ではありません**（`body { position: fixed }` は据え置き）。工事4c §7-4 の申し送りはそのまま有効です
