# 本番反映 報告 — 工事6・工事7 を main に取り込み

日付: 2026-09-25 ／ 作業: v2 → main の fast-forward と push のみ（コードの変更なし。Firebase のルール・Vercel の設定は触っていません）

## 1. 結果（ひとことで）

main を **`dfa5355` → `acdd387`** に fast-forward で進め、push しました（強制 push なし）。

Vercel の Production デプロイは success（Deployment has completed）になりました。本番 URL は新しいアイコン・manifest を返しています（§5）。

## 2. 各手順の実行結果

**手順0 事前確認**（`git fetch origin` の後）

| 参照 | ハッシュ |
|---|---|
| origin/main | `dfa53550d6f5a3c43d295181c465d6f4fee7e113` |
| origin/v2 | `acdd3873e67dcdfeae2ae642275c8219ba8a8efd` |
| main（ローカル） | `dfa53550d6f5a3c43d295181c465d6f4fee7e113` |
| v2（ローカル） | `acdd3873e67dcdfeae2ae642275c8219ba8a8efd` |

- ブランチ: v2
- `git merge-base --is-ancestor origin/main v2`: 真
- `git log --oneline dfa5355..v2`: 7 行（期待どおり）
- `git diff --ignore-cr-at-eol --stat`: 空。未追跡ファイル（`git ls-files --others --exclude-standard`）: なし

**手順1 `git checkout main` → `git merge --ff-only v2`**
- 出力: `Updating dfa5355..acdd387` / `Fast-forward`
- `18 files changed, 1488 insertions(+), 1 deletion(-)`
- 追加 15、変更 3、**削除 0**（`git diff --name-status dfa5355 main` を種類ごとに数えた結果: A 15、M 3）

**手順2 `git push origin main`**
- 出力: `dfa5355..acdd387  main -> main`（終了コード 0。拒否なし）

**手順3 `git checkout v2`**
- `Switched to branch 'v2'` / `Your branch is up to date with 'origin/v2'.`

**手順4 最終確認**（`git fetch origin` の後）

| 参照 | ハッシュ |
|---|---|
| main | `acdd387` |
| v2 | `acdd387` |
| origin/main | `acdd387` |
| origin/v2 | `acdd387` |

- 4 つとも同じです
- 作業ツリーはクリーンです（`git diff --ignore-cr-at-eol --stat` が空、未追跡ファイルなし）

## 3. 今回 main に入った工事

`git log --oneline dfa5355..acdd387`:

```
acdd387 工事7の報告書を追加
d33da28 index.html にアイコンとmanifestのタグを足し、テストを追加
0f5d120 アイコンの原本SVGとPNG 4枚、生成スクリプトを追加
387afc5 工事6の報告書を追加
60e3701 store.js の書き込みを偽 Firebase で固定するテストを追加
2f5c0ac 仕様書 §5.2 のグループ一覧を並び順の記述に直す
9620294 本番反映（工事4d・5）の報告書を追加
```

- 工事6: 仕様書 §5.2 の修正、`tests/store.test.js`（store.js の書き込みの固定テスト 40 件）、CLAUDE.md の 1 行
- 工事7: アイコン（icons/ の SVG と PNG 4 枚）、`site.webmanifest`、index.html の head の 6 タグ、`tests/icons.test.js`（24 件）
- あわせて入ったもの: 前回の本番反映（工事4d・5）の報告書（`9620294`）。v2 にだけコミットしていたもので、今回の fast-forward で main にも入りました

`git diff --stat dfa5355 acdd387` の要約（18 ファイル、+1488 / −1、**削除されたファイルは無し**）:

| 種類 | ファイル |
|---|---|
| 画面に効く変更 | `index.html`（+6 行。head のタグのみ）、`site.webmanifest`（新規）、`icons/icon.svg`・`apple-touch-icon.png`・`favicon-32.png`・`icon-192.png`・`icon-512.png`（新規） |
| テスト | `tests/store.test.js`（新規）、`tests/icons.test.js`（新規） |
| 書類 | `CLAUDE.md`（+1 行）、`docs/10_仕様書_v2再設計.md`（§5.2 の 1 行）、`docs/reports/工事6_報告.md`・`工事7_報告.md`・`本番反映_工事4d-5_報告.md`（新規） |
| 道具 | `tools/make_icons.py`・`edit_index_icons_7.py`・`edit_spec_sidebar_6.py`・`edit_claude_md_storetest_6.py`（新規） |

`js/` と `css/` の変更はありません。

## 4. 止まる条件に当たったかどうか

どれにも当たっていません。
- origin/main: `dfa5355`（期待どおり）
- origin/v2: `acdd387`（期待どおり）
- 未コミットの変更・未追跡ファイル: なし
- `--ff-only`: 成功
- push: 拒否なし

## 5. デプロイの確認結果（読み取りのみ）

**Vercel**（`gh api repos/oukauog/kinsen-kanri/commits/acdd387/statuses` と `deployments?sha=…`）
- Production のデプロイ（ID 6661928044、Vercel の DsJfLW7RtdPQ6PSiPPTkBpgxNYwk）は、13:34:36Z に pending、13:34:41Z に **success**（Deployment has completed）になりました
- 同じコミットの Preview デプロイ（ID 6661723462、13:24:11Z に success）は、工事7 の push のときのものです

**本番 URL https://kinsen-kanri.vercel.app/**

HTML（トップページ）に含まれるタグを数えた結果です。

| 文字列 | 出現数 |
|---|---|
| icons/apple-touch-icon.png | 1 |
| site.webmanifest | 1 |
| apple-mobile-web-app-title | 1 |
| icons/icon.svg | 1 |
| icons/favicon-32.png | 1 |
| theme-color | 1 |
| apple-mobile-web-app-capable | **0**（全画面モードのタグは無い） |

各ファイルの応答です。

| パス | 状態 | Content-Type | サイズ |
|---|---|---|---|
| / | 200 | text/html; charset=utf-8 | 28,508 バイト |
| /icons/apple-touch-icon.png | 200 | image/png | 4,686 バイト |
| /icons/icon.svg | 200 | image/svg+xml | 479 バイト |
| /site.webmanifest | 200 | application/manifest+json; charset=utf-8 | 348 バイト |
| /icons/favicon-32.png | 200 | image/png | 909 バイト |
| /icons/icon-192.png | 200 | image/png | 4,339 バイト |
| /icons/icon-512.png | 200 | image/png | 7,316 バイト |
| /js/group_order.js | 200 | application/javascript; charset=utf-8 | 4,654 バイト（これまでどおり） |

- 本番の `/site.webmanifest` は JSON として読めました。short_name は「金銭管理」、display は「browser」、icons は 192x192 と 512x512 です
- 本番の `/icons/apple-touch-icon.png` の md5 は、リポジトリの同じファイルと一致しました（`faf9b4c9…`）

## 6. 花井の確認手順

本番 URL: https://kinsen-kanri.vercel.app/

**PC（Chrome など）**
1. 本番 URL を開く → タブに、藍色の四角に白い「¥」の favicon が出ること。古い表示のままなら、タブを閉じて開き直すか Ctrl+Shift+R
2. 今までどおりログインして使えること

**iPhone の Safari**
3. **プレビュー URL（…git-v2…vercel.app）で追加したホーム画面のアイコンは、本番とは別物です。** 本番用には改めて追加し直してください。プレビューの方はホーム画面から消してかまいません。以前に本番 URL を追加していた場合も、古いアイコン（スクリーンショット）を覚えていることがあるので、一度消してから追加し直してください
4. Safari で本番 URL を開く → 下の「共有」→「ホーム画面に追加」→ 候補に**藍色の ¥ のアイコン**と名前「**金銭管理**」が出ること（末尾が切れない）→「追加」
5. ホーム画面のアイコンから開く → **今までどおり Safari の画面のまま**開くこと（上に URL の欄、下にツールバー。全画面にはならない）
6. その状態で Google ログインが最後まで通り、グループが表示されること

**データ**
7. 広島旅行を開き、**支払いの件数・残高・過去の清算が反映前と同じ**であること（今回は画面の動きとデータの書き方を変えていません。見るだけにして、操作はしないでください）

## 7. 戻し方

- Vercel の Deployments で、前の本番（`dfa5355`「工事5の報告書と仕様書 §3 の並び順を追加」）のデプロイを選び、**Promote to Production**
- Firebase のルールは今回触っていないので、戻す作業は不要です
- 戻した場合、index.html からアイコンのタグが無くなるだけで、データには影響しません。ホーム画面に追加済みのアイコンは iOS 側に残ります
- git の main を戻す必要が出た場合は、相談役と相談のうえで別途行います（今回は何もしていません）

## 8. 気づいた点

- この報告書は v2 にだけコミットして push します。そのため main は `acdd387` のまま、v2 がこの報告書の 1 コミット分だけ先に進みます（前回と同じ扱い）
- 本番の `site.webmanifest` の Content-Type は `application/manifest+json; charset=utf-8` で、vercel.json を触らずに正しく配られています
- 本番の `icons/icon.svg` は 479 バイトです。リポジトリの中身（LF）と同じ内容で、表示に影響はありません
- ホーム画面のアイコンと favicon は、端末やブラウザが長く覚えていることがあります。もし反映が見えないときは、§6 の 1・3 のとおり開き直す・追加し直すで確かめてください
