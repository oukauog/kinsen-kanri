# 工事4d 報告 — 過去の清算の表示 2 点と、報告書 2 本の制御文字

作業ブランチ: `v2`（main には触っていません）／ 日付: 2026-09-25

## 1. 結果（ひとことで）

3 点とも直して v2 に push しました。
- 報告書 2 本の生の制御文字を文字表記に直し、git がテキストとして扱うようになりました
- チェック済みの送金で、打ち消し線が「A → B」だけに付き、「✓ 名前 日時」には付かなくなりました（PC・スマホ共通）
- スマホ幅では「✓ 名前 日時」を名前の下の行に出すようにしました。**ただし、横にはみ出す現象はヘッドレス Chrome（375px）では再現しませんでした**（修正前から scrollWidth = clientWidth）。iOS Safari 固有の可能性があるため、iPhone 実機での確認をお願いします（§7）

## 2. 手順0 の結果（報告書 2 本の制御文字）

`tools/fix_report_ctrl_4d.py` を `tools/fix_migrate_ctrl_4b.py` と同じ作りで作成・実行しました。生の制御文字を 1 バイトずつ「バックスラッシュ + x + 16 進 2 桁」の文字列に置き換えています（例: NUL → `\x00`、0x1F → `\x1f`、0x7F → `\x7f`）。文章は変えていません。置換前の数が調査時の数と一致すること、制御文字が現れる行が想定の行（`Migrate.isSafeKey` / `BAD_KEY` / 「エスケープ表記」を含む行）だけであること、置換後に 0 個であること、を確かめてから書き戻し、書き込み後に読み直して数え直しています。

| ファイル | 置換前 | 置換後 | サイズ |
|---|---|---|---|
| 工事4b_報告.md | NUL 1、0x7F 1（計 2） | 0 | 14859 → 14865 バイト |
| 本番切替_報告.md | NUL 3、0x1F 2、0x7F 2（計 7） | 0 | 5449 → 5470 バイト |

- 数えた範囲は依頼どおり 0x00〜0x08、0x0B、0x0C、0x0E〜0x1F に、DEL（0x7F）を足しています。0x7F は依頼の範囲外ですが、同じ説明箇所に入っていた生の制御文字（文章では `\x7f` と書いたつもりの箇所）なので、あわせて文字表記にしました
- 依頼文の「NUL 1・その他 2」「NUL 3・その他 7」は、実測では上の表の「計」と一致したので、「その他」は合計の意味と解釈しました
- git diff: この修正のコミット自体は、変更前の側に NUL があるため `git diff --stat` では「Bin」と出ます（変更前後の片方でもバイナリならそう出るため、避けられません）。`git diff --text` では置換した行が文字として読めることを確認しました。変更後のファイルは `git diff --no-index --numstat` で行数が出る（本番切替 106 行、工事4b 181 行）＝テキスト扱いで、`git grep -I` の対象にも入るようになりました（コミット前は docs/reports の 9 本中 7 本、コミット後は 9 本すべて）。次に 2 本を直すときからは普通の差分が出ます
- コミット: `cad3030`

## 3. A の変更内容（打ち消し線）

**DOM（js/ui.js、過去の清算の `transfers.map` の中、834 行付近）**

変更前: `<span class="transfer-names">A → B<span class="transfer-by">✓ 名前 日時</span></span>`

変更後: `<span class="transfer-names"><span class="transfer-pair">A → B</span><span class="transfer-by">✓ 名前 日時</span></span>`

`.transfer-by` は `.transfer-pair` の外（`.transfer-names` の直下、`.transfer-pair` の後ろ）です。未チェックの行は `.transfer-by` が無いだけで、形は同じです。

**CSS（css/v2.css）**
- 380 行: `.transfer-row.done .transfer-names { text-decoration: line-through; color: var(--gray-500); }` → `.transfer-row.done .transfer-names { color: var(--gray-500); }`（灰色は名前全体のまま）
- 381〜382 行を追加: 理由のコメント 1 行と `.transfer-row.done .transfer-pair { text-decoration: line-through; }`
- 旧 455 行 `.transfer-row.done .transfer-by { text-decoration: none; }` を削除（効いていなかったもの）

編集スクリプト: `tools/edit_ui_strike_4d.py`、`tools/edit_css_strike_4d.py`、`tools/edit_uismoke_strike_4d.py`（テスト）。

「清算を確定する」側（今回の清算の送金一覧、786 行付近）は `.settlement-item` / `.settlement-from` などの別クラスで、`.transfer-*` を使っていません。今回の変更で見た目は変わりません。

コミット: `1090bbf`

## 4. B の測定結果と変更内容（スマホで横に切れる）

### 測定のしかた
`tools/make_uitest.py` の確認用ページをヘッドレス Chrome で開き、`tests/ui_smoke_driver.js` の中で次の状態を作りました: グループ「テスト横はみ出し」（メンバー おふらいん・ものらる・m20nde）で 123,456 円の支払いを 1 件入れて清算を確定し、送金 2 本を両方チェック済みにする（「おふらいん → ものらる ✓ テスト太郎 09/25 09:16」の形）。その「清算」モーダルの DOM を、幅 375px / 1024px の iframe に写して開き、各要素の scrollWidth / clientWidth を測りました（行は複数あるので、いちばんはみ出しの大きいものを代表値にしています）。

### 結果（scrollWidth / clientWidth、単位 px）

| 幅 | 時点 | モーダル本体 .modal | .past-settlement | .transfer-row | .transfer-names | モーダルの左右端 | 「✓ 名前 日時」の位置 |
|---|---|---|---|---|---|---|---|
| 375 | 修正前（工事4d 着手時） | 335 / 335 | 285 / 285 | 261 / 261 | 258 / 182 | 20〜355（画面 375） | 名前と同じ行 |
| 375 | A のあと・B の前 | 335 / 335 | 285 / 285 | 261 / 261 | 258 / 182 | 20〜355 | 名前と同じ行 |
| 375 | 修正後（B のあと） | 335 / 335 | 285 / 285 | 261 / 261 | 182 / 182 | 20〜355 | **名前の下の行** |
| 1024 | 修正前（工事4d 着手時） | 560 / 560 | 510 / 510 | 480 / 480 | 394 / 394 | 232〜792（画面 1024） | 名前と同じ行 |
| 1024 | A のあと・B の前 | 560 / 560 | 510 / 510 | 480 / 480 | 394 / 394 | 232〜792 | 名前と同じ行（名前 142px・印 128px） |
| 1024 | 修正後（B のあと） | 560 / 560 | 510 / 510 | 480 / 480 | 394 / 394 | 232〜792 | 名前と同じ行（名前 142px・印 128px） |

ページ全体（documentElement）も全時点で 375/375、1024/1024 でした。

### 読み取れること
- **ヘッドレス Chrome ではモーダル・.past-settlement・.transfer-row のどれも、修正前からはみ出していません（再現しませんでした）**
- 修正前の 375px では `.transfer-names` の中身が 258px あって 182px の枠に入らず、`overflow: hidden` + `…` で**「✓ 名前 日時」の後半が切れて見えなくなっていました**。Chrome ではこれが「省略」で済み、iOS Safari では「横にはみ出す」に見えている可能性がありますが、実機でないと判別できません
- 横スクロールの原因（min-width、nowrap の連鎖、padding 分の超過など）は、測定では見つかりませんでした。したがって、その種の修正はしていません
- PC 幅（1024px）の数値は修正前・後で一致しています（A のあとで測れるようになった名前 142px・印 128px も、B の前後で同じ）

### 変更内容（css/v2.css の @media (max-width: 640px) の中だけ）
- `.transfer-by`: `display: block; margin-left: 0;`（名前の下の行に置く）。文字サイズ 0.65rem はそのまま。長いときは 1 行のまま `…` で省略（`overflow: hidden; text-overflow: ellipsis;`、nowrap は元の規則のまま）
- `.transfer-pair`: `display: block; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`（これまで `.transfer-names` に付けていた nowrap + ellipsis を移した。text-overflow は block の要素にしか効かないため block にしています）
- `.transfer-names`: `overflow: hidden;` だけ残す（中身が行の外へ出ないための保険）

641px 以上の規則には触れていません。編集スクリプト: `tools/edit_css_pastwidth_4d.py`、`tools/edit_uismoke_pastwidth_4d.py`（測定とテスト）。

コミット: `d760498`

## 5. テスト結果

- `node tests/run_all.js`: **6 ファイルすべて通過**（authdomain 8、calc 16、csv 25、env 16、migrate 26、settle 25 ＝ 116 件）
- 画面の通し確認（ヘッドレス Chrome、`UI-SMOKE-PASS`）: **166 件すべて通過**（工事4d 前 156 件 + A 4 件 + B 6 件）
- 配線チェック `tools/check_wiring.py`: OK

追加した確認（tests/ui_smoke_driver.js）:

A（工事4b-B の送金チェックの確認の直後）
- チェック済みの行: 「A → B」（.transfer-pair）に打ち消し線が付く（computed の textDecorationLine に line-through）
- チェック済みの行: .transfer-by 自身には打ち消し線が付かない
- チェック済みの行: .transfer-by は .transfer-pair の外（.transfer-names の直下、後ろ）にある（DOM で確認）
- チェック済みの行: .transfer-by の祖先（行まで）に打ち消し線が無い（線が伝わらない）

B（新しいグループ「テスト横はみ出し」）
- 幅の確認用: 送金が 2 本ある
- 幅の確認用: チェック済みで名前と日時が付いた行がある
- スマホ幅 375px: 過去の清算の行がはみ出さない（モーダル・.past-settlement・.transfer-row、とモーダル右端 ≦ 画面幅）
- スマホ幅 375px: 「✓ 名前 日時」は名前の下の行に出る
- PC 幅 1024px: 過去の清算の行がはみ出さない
- PC 幅 1024px: 「✓ 名前 日時」は今までどおり名前と同じ行

あわせて、測定用の iframe（名前 `kk-measure`）の中では通し確認そのものを走らせないようにしました（`run()` の先頭で抜ける）。測った数字は結果に `info` 行として残ります。

A の 4 件は修正前の DOM では失敗すること（.transfer-pair が無い）、B の「名前の下の行」は B の前で失敗することを確認しています。「はみ出さない」の 2 件は、上のとおり修正前から通ります（ヘッドレスで再現しないため、再現テストにはなっていません）。

`_uitest_tmp.html` は確認のたびに消しており、コミットしていません。

## 6. コミット一覧（すべて v2、push 済み）

| ハッシュ | 内容 |
|---|---|
| `cad3030` | 報告書 2 本の制御文字を文字表記に直す |
| `1090bbf` | 清算済みの送金で打ち消し線をA→Bだけにする |
| `d760498` | スマホ幅で送金のチェック者を名前の下の行に出す |
| （このファイル） | 工事4dの報告書を追加 |

## 7. 花井の確認手順

プレビュー URL: https://kinsen-kanri-git-v2-o-ka-s-projects.vercel.app/ （グループは「テスト」で始まるものを使ってください。無ければ「テスト4d」などで作成）

準備（PC でも iPhone でも可）
1. ログインして「テスト…」のグループを開く。メンバーは 3 人程度、名前は「おふらいん」「ものらる」くらいの長さがあると再現に近くなります
2. 支払いを 1 件入れて（例: 12,345 円）「清算」→「この内容で清算する」
3. 「過去の清算」の送金のうち 1 本にチェックを入れる（もう 1 本はチェックしないまま）

iPhone の Safari
4. 「清算」モーダルを開き、チェックした行を見る
   - 1 行目に「おふらいん → ものらる」、その**下の行**に小さく「✓ 名前 09/25 09:16」が出ること
   - 横線は 1 行目の「おふらいん → ものらる」だけに付き、「✓ 名前 日時」には付かないこと
   - 右端の金額が切れずに見えること、モーダルを左右に指でずらしても横にずれない（横スクロールしない）こと
   - チェックしていない行は 1 行のままで、線も印も無いこと
5. 名前が長いメンバーがいる場合、名前が「…」で省略されて 1 行に収まること
6. はみ出しが残っていたら、どの行（ヘッダー・送金行・下のボタン）がはみ出しているかを教えてください。ヘッドレスでは再現できていないため、次の手掛かりになります

PC（Chrome など、ウィンドウ幅は広めで）
7. 同じ「清算」モーダルで、チェックした行が今までどおり**1 行**（「おふらいん → ものらる ✓ 名前 日時」の順、金額は右端）であること
8. 横線が「おふらいん → ものらる」だけに付き、「✓ 名前 日時」には付かないこと
9. 上の「今回の清算」（確定前の送金一覧）の見た目が変わっていないこと

## 8. 気づいた点（直していないもの）

- **iOS で横にはみ出す原因は未特定**です。ヘッドレス Chrome ではモーダル・行ともはみ出さず、代わりに `.transfer-names` の中身が `…` で切られて「✓ 名前 日時」の後半が見えなくなっていました（§4）。今回の 2 段表示でその切れ方は解消しますが、iOS 固有の原因（例えば文字の幅の違い、`<label>` を flex にした行の最小幅の扱いの差）が別にあれば残る可能性があります。実機検収の結果を待ちたいです
- `.modal` は `width: 100%; margin: 20px;`（index.html 98 行）で、幅 100% と左右の margin を足すと画面幅を超える指定です。Chrome では flex の縮みで 335px に収まっていて、測定上の問題は出ていません。今回は測定で原因と確認できなかったので触っていません
- 手順0 の修正コミット自体は、`git diff --stat` では「Bin」と表示されます（変更前の側に NUL があるため。§2）。次の変更からは普通の差分になります
- 作業中に 1 度、編集スクリプト `tools/edit_css_pastwidth_4d.py` を Git Bash のヒアドキュメントで作ってしまいました（日本語を含むファイルを Python / Write ツール以外で書かない、というルールから外れています）。作成後にバイト列を UTF-8 として読み直し、置換文字（U+FFFD）が無いこと・BOM が無いこと・v2.css に書き込まれたコメントが化けていないことを確認しています。内容に問題はありません
- ヘッドレス Chrome を毎回同じプロファイルで起動すると、`py -m http.server` から配った古い `js/ui.js` がキャッシュされて、変更前の画面で確認してしまうことがありました（今回 1 度発生し、実行し直しています）。通し確認は、毎回新しいプロファイル（`--user-data-dir` に新しい一時フォルダ）で起動すると確実です。ルールに入れるかは相談役の判断にお任せします
